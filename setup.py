from setuptools import find_packages
from setuptools import setup

setup(name="snarxiv-spotter",
      version="0.0",
      dscription="Project to fine-tune a language model to beat the snarxiv vs arxiv game",
      packages=find_packages()
      )